type Status {
  untrackedFiles: [file]
  modifiedFiles: [file]
  stagedFiles: [file]
}

type file {
  name: String!
}

type Existence {
  exists: Boolean!
  message: String!
}

type FileCommitChangePayload {
  filename: String!
  status: Boolean!
  error: String
}

input FileCommitChangeInput {
  filename: String!
  directory: String!
}
